# Copy this file to .env and adjust as needed.
# All values below are the application's own defaults (see app/config.py)
# -- this file exists so the required variables are documented, not
# because the app fails to start without it.

MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=groww_watchlist
POLL_INTERVAL_SECONDS=60
